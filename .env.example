# Admin backend API base URL (no trailing slash).
# Required in production. In development, defaults to http://localhost:4005 if unset.
# Example for Vercel: set in Project → Settings → Environment Variables:
#   VITE_API_URL = https://your-admin-api.vercel.app
VITE_API_URL=http://localhost:4005
